<h2>Encrypted Dropbox</h2>
<p class="lead">Upload a document here. It will be encrypted with your public key, so only the website owner can read it.</p>

<div class="form-group">
<label class="custom-file">
    <input type="file" id="file2" class="custom-file-input">
    <span class="custom-file-control"></span>
</label>
</div>
<p class="lead">Your private key is stored in {$CurrentMember.RSAPublicKey.GUID}.txt</p>
